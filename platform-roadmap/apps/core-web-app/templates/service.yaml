apiVersion: v1
kind: Service
metadata:
  name: {{ include "core-web-app.fullname" . }}
  labels:
    {{- include "core-web-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "core-web-app.selectorLabels" . | nindent 4 }}
