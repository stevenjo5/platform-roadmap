apiVersion: v1
kind: Service
metadata:
  name: platform-nodeport-service
spec:
  type: NodePort
  selector:
    app: resilient-web
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 32000
