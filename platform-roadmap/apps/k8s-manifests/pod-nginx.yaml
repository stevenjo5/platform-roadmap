apiVersion: v1
kind: Pod
metadata:
  name: platform-core-pod
  labels:
    app: web-tier
    tier: production
spec:
  containers:
    - name: web-container
      image: nginx:alpine
      ports:
        - containerPort: 80
