apiVersion: v1
kind: PersistentVolume
metadata:
  name: platform-local-pv
  labels:
    type: local-storage
spec:
  storageClassName: manual
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/data"
